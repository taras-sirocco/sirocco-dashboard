/**
 * Прапорці функцій для планшетного застосунку. Не з env — це не секрет і не
 * per-deploy налаштування, а вимкнений шматок продукту, який свідомо лишили
 * в коді для повернення пізніше. Щоб увімкнути назад — зміни на `true`.
 */

/**
 * Покрокова інструкція (TWI), звірка з еталоном і фото в потоці задач —
 * прибрано з планшета за рішенням 2026-09-14 (замінено на сітку карток
 * з інлайн-вводом кількості). Дані (`tasks.steps`, `tasks.referenceNote`,
 * `stepMedia`) з бази НЕ видалені, лишаються редагованими в адмінці.
 */
export const STEP_INSTRUCTIONS_ENABLED = false
